name: deploy

on:
  workflow_dispatch:
    inputs:
      region:
        required: true
        type: string
      env:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Echo variables
